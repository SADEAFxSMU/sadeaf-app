{
  "private": true,
  "name": "sadeaf-hasura",
  "version": "1.0.0",
  "scripts": {
    "console": "docker-compose up -d --build && sh ./wait-for-hasura-console"
  },
  "dependencies": {},
  "devDependencies": {
    "hasura-cli": "1.3.0"
  }
}
